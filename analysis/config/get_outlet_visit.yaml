visit_latency: 5 # in days

start_date: '2026-01-08'
end_date: '2026-04-01'  # exclusive

paths:
 sql_template:
  get_15min_visit: 'SQL_templates/visits/extract_15min_interval_non-null_visits.sql'
 export_directory: 'visits'    # for local run only

filename_base: 'pp_consolidated_visit_15min'
